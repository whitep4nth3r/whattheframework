---
---

<style is:global>
  :root {
    --black: #1b1b1b;
    --rainbow1: #ffd20e;
    --rainbow2: #e5bc08;
    --rainbow3: #ec8401;
    --rainbow4: #e65c04;
    --rainbow5: #e52e06;
    --rainbow6: #de006b;
    --rainbow7: #ca039e;
    --rainbow8: #a203cb;
    --rainbow9: #6d01c9;
    --rainbow10: #22008f;
    --rainbow11: #062f9a;
    --rainbow12: #0045ac;
    --rainbow13: #007db2;
    --rainbow14: #00b8d9;
  }

  * {
    box-sizing: border-box;
    margin: 0;
  }

  html {
    font-size: 100%;
  }

  body {
    font-size: 1rem;
    margin: 0;
  }

  .grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    align-items: stretch;
    padding: 1rem;
  }

  .card {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    flex-grow: 1;
    background: radial-gradient(73.46% 87.33% at 0% 0%, rgba(255, 255, 255, 0.08) 0%, rgba(255, 255, 255, 0) 100%),
      linear-gradient(125deg, rgba(255, 255, 255, 0.8) 0%, rgba(229, 217, 236, 0.8) 100%);
    backdrop-filter: blur(1.25rem);
    border-radius: 2rem;
    padding: 1.5rem;
    text-decoration: none;
    color: var(--black);
    font-size: 1.5rem;
    line-height: calc(32 / 24);
  }

  @media screen and (min-width: 50rem) {
    .card {
      min-height: 25rem;
    }
  }

  .card__examples {
    display: flex;
    flex-wrap: wrap;
    flex-direction: row;
    gap: 0.5rem;
  }

  .card__examplesPill {
    padding: 0.25rem 0.75rem;
    background: #f2f4f7;
    mix-blend-mode: multiply;
    border-radius: 1rem;
    font-size: 0.875rem;
    line-height: 100%;
  }

  .wrapper {
    margin: 0 auto;
    max-width: 1184px;
  }
</style>

<style>
  :root {
    --header-height: 6.25rem;
  }

  header {
    background-color: var(--black);
    margin: 0;
    height: var(--header-height);
  }

  main {
    --stop-size: calc(100vh - var(--header-height)) / 14;
    min-height: calc(100vh - var(--header-height));
    background-repeat: no-repeat;
    background: linear-gradient(
      to bottom,
      var(--rainbow1) calc(var(--stop-size)),
      var(--rainbow2) calc(var(--stop-size)) calc((var(--stop-size)) * 2),
      var(--rainbow3) calc((var(--stop-size)) * 2) calc((var(--stop-size)) * 3),
      var(--rainbow4) calc((var(--stop-size)) * 3) calc((var(--stop-size)) * 4),
      var(--rainbow5) calc((var(--stop-size)) * 4) calc((var(--stop-size)) * 5),
      var(--rainbow6) calc((var(--stop-size)) * 5) calc((var(--stop-size)) * 6),
      var(--rainbow7) calc((var(--stop-size)) * 6) calc((var(--stop-size)) * 7),
      var(--rainbow8) calc((var(--stop-size)) * 7) calc((var(--stop-size)) * 8),
      var(--rainbow9) calc((var(--stop-size)) * 8) calc((var(--stop-size)) * 9),
      var(--rainbow10) calc((var(--stop-size)) * 9) calc((var(--stop-size)) * 10),
      var(--rainbow11) calc((var(--stop-size)) * 10) calc((var(--stop-size)) * 11),
      var(--rainbow12) calc((var(--stop-size)) * 11) calc((var(--stop-size)) * 12),
      var(--rainbow13) calc((var(--stop-size)) * 12) calc((var(--stop-size)) * 13),
      var(--rainbow14) calc((var(--stop-size)) * 13) calc((var(--stop-size)) * 14)
    );
  }
</style>

<meta charset="utf-8" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width" />
<meta name="generator" content={Astro.generator} />
<title>What the Framework?</title>

<body>
  <header>what the framework?</header>
  <main>
    <div class="wrapper">
      <slot />
    </div>
  </main>
</body>
